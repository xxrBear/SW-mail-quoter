[project]
name = "test-email"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "lxml>=5.4.0",
    "pandas>=2.0.3",
    "python-dotenv>=1.0.1",
]
